# Fixed GIDs so the setgid binaries baked into the image always match the group on the
# installed system, even after /etc/group has been modified locally.
g onepassword     880
g onepassword-cli 881
g onepassword-mcp 882
